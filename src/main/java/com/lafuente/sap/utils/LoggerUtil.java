package com.lafuente.sap.utils;

import java.util.logging.Level;
import java.util.logging.Logger;
import org.apache.commons.lang3.StringUtils;

/**
 *
 * @author GUIDO CACERES PINTO
 */
public class LoggerUtil {

    /*public static void i(String className, String message) {
        Logger.getLogger(StringUtils.isEmpty(className) ? "API-SAP" : className)
                .log(Level.INFO, message);
    }

    public static void e(String className, String message) {
        Logger.getLogger(StringUtils.isEmpty(className) ? "API-SAP" : className)
                .log(Level.SEVERE, message);
    }

    public static void e(String className, Throwable ex) {
        Logger.getLogger(StringUtils.isEmpty(className) ? "API-SAP" : className)
                .log(Level.SEVERE, ex == null ? "Error desconocido." : ex.getMessage());
    }*/
}
